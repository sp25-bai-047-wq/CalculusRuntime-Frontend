Derivative of $x^7$ is:
If $f(x)=3x^2-5x+1$, then $f'(2)$ equals:
$(uv)'$ equals:
$\\dfrac{d}{dx}\\sin(3x)$ equals:
$\\dfrac{d}{dx}\\ln(5x)$ for $x>0$ equals:
Quotient rule for $f=u/v$ is:
$\\dfrac{d}{dx}e^{2x}$ equals:
If $y=\\cos x$, then $y'$ equals:
$\\dfrac{d}{dx}(x^2\\sin x)$ equals:
$\\dfrac{d}{dx}\\tan x$ equals:
Chain rule: $\\dfrac{d}{dx}f(g(x))$ equals:
If $f(x)=1/x$, then $f'(x)$ equals:
$\\dfrac{d}{dx}\\sqrt{x}$ equals:
If $h(x)=\\dfrac{x^2+1}{x}$, then $h'(x)$ equals:
$\\dfrac{d}{dx}\\ln(\\cos x)$ on $(-\\pi/2,\\pi/2)$ equals:
If $A=\\pi r^2$ and $dr/dt=2$, then at $r=3$, $dA/dt$ equals:
Critical points of differentiable $f$ occur where:
If $f'(c)=0$ and $f''(c)<0$, then at $c$:
$f$ is increasing on an interval when:
L'Hôpital applies to which indeterminate forms?
Evaluate $\\lim_{x\\to 0}\\dfrac{\\sin x-x}{x^3}$ using L'Hôpital as needed.
Absolute extrema of continuous $f$ on $[a,b]$ occur at:
For $f(x)=x^3-3x$, local min occurs at:
A ladder $10$ ft slides: base moves away at $2$ ft/s. When base is $6$ ft, height drops at:
Concavity upward means:
Maximize product $xy$ given $x+y=10$. Max product is:
Inflection candidate where:
Related rates: differentiate the constraint:
If $f'(x)=x^2(x-2)$, then $f$ has a local:
Evaluate $\\lim_{x\\to\\infty}\\dfrac{\\ln x}{x}$.
Implicitly, for $x^2+y^2=25$, $dy/dx$ equals:
MVT says there is $c\\in(a,b)$ with $f'(c)$ equal to:
Rolle's theorem additionally needs:
Logarithmic differentiation is handy for:
If $y=x^x$ ($x>0$), then $y'$ equals:
Linear approximation of $f$ near $a$ is:
Second derivative of $\\sin x$ is:
If $x=\\cos t$, $y=\\sin t$, then $dy/dx$ equals:
Differential $dy$ equals:
Derivative of $\\arcsin x$ is:
For $e^y=x$, $dy/dx$ equals:
Higher-order: if $f(x)=e^{2x}$, then $f''(x)$ equals:
Approximate $\\sqrt{9.1}$ using $f(x)=\\sqrt{x}$ at $a=9$:
Implicit $xy=1$ gives $y'$ equal to:
If $f$ is continuous on $[a,b]$ and differentiable on $(a,b)$ with $f(a)=f(b)$, Rolle guarantees:
$\\int 3x^2\\,dx$ equals:
$\\int_0^2 3x^2\\,dx$ equals:
FTC: $\\dfrac{d}{dx}\\int_0^x f(t)\\,dt$ equals:
$\\int\\cos x\\,dx$ equals:
Average value of continuous $f$ on $[a,b]$ is:
$\\int 2x\\cos(x^2)\\,dx$ equals:
Area between $y=x$ and $y=x^2$ on $[0,1]$ equals:
$\\int e^{3x}\\,dx$ equals:
FTC II: $\\int_a^b f=F(b)-F(a)$ when $F'=f$ and:
$\\int_1^e\\dfrac{1}{x}\\,dx$ equals:
Indefinite integrals differ by:
$\\int_0^{\\pi/2}\\sin x\\,dx$ equals:
Substitution $u=g(x)$ converts $\\int f(g(x))g'(x)\\,dx$ into:
If $F(x)=\\int_2^{x^2}\\sin t\\,dt$, then $F'(x)$ equals:
$\\int(4x-1)\\,dx$ equals:
Integration by parts uses:
$\\int\\ln x\\,dx$ equals:
$\\int_1^{\\infty}x^{-2}\\,dx$ converges to:
Partial fractions for proper rational functions need:
$\\int\\dfrac{1}{x^2+1}\\,dx$ equals:
Trig sub for $\\sqrt{a^2-x^2}$ often uses:
$\\int\\sin^2 x\\,dx$ strategy starts with:
Disk method volume about $x$-axis for $y=f(x)$ from $a$ to $b$:
$\\int xe^{x}\\,dx$ equals:
Arc length of $y=f(x)$ on $[a,b]$ is:
$\\int_0^{\\infty}e^{-x}\\,dx$ equals:
Decompose $\\dfrac{1}{(x-1)(x+2)}$ as:
Trig integral $\\int\\sin^3 x\\,dx$ uses:
Washer method includes:
$\\int\\sec x\\tan x\\,dx$ equals:
Evaluate $\\lim_{x\\to 3}\\dfrac{x^2-9}{x-3}$.
Evaluate $\\lim_{x\\to 0}\\dfrac{\\sqrt{x+4}-2}{x}$.
For $f(x)=\\begin{cases}2x+1&x<1\\\\3&x\\ge 1\\end{cases}$, $\\lim_{x\\to 1^-}f(x)$ equals:
If $|f(x)|\\le x^2$ near $0$, then $\\lim_{x\\to 0}f(x)$ equals:
Evaluate $\\lim_{x\\to 2}\\dfrac{x^2-5x+6}{x-2}$.
$f$ is continuous at $a$ when:
Evaluate $\\lim_{x\\to 4}\\dfrac{x-4}{\\sqrt{x}-2}$.
The jump discontinuity of $g(x)=\\begin{cases}0&x<0\\\\1&x\\ge 0\\end{cases}$ at $0$ occurs because:
Evaluate $\\lim_{x\\to -2}\\dfrac{x^2-4}{x+2}$.
Evaluate $\\lim_{x\\to 0}\\dfrac{\\sin(5x)}{x}$.
For removable discontinuity at $c$, which must be true?
Evaluate $\\lim_{x\\to 1}\\dfrac{x^3-1}{x-1}$.
$\\lim_{x\\to 0^+}\\dfrac{1}{x}$ equals:
Evaluate $\\lim_{x\\to 9}\\dfrac{x-9}{\\sqrt{x}-3}$.
If $\\lim_{x\\to a}f(x)=L$ and $\\lim_{x\\to a}g(x)=M$, then $\\lim(f+g)$ equals:
Evaluate $\\lim_{x\\to\\infty}\\dfrac{3x^2+1}{x^2-5}$.
$\\lim_{x\\to\\infty}\\dfrac{\\sin x}{x}$ equals:
A horizontal asymptote $y=L$ means:
Evaluate $\\lim_{x\\to 0}\\dfrac{1-\\cos x}{x^2}$.
IVT guarantees a root of continuous $f$ on $[a,b]$ when:
Evaluate $\\lim_{x\\to\\infty}\\left(\\sqrt{x^2+x}-x\\right)$.
Vertical asymptote of $f(x)=\\dfrac{1}{x-2}$ occurs at:
Evaluate $\\lim_{x\\to 0}\\dfrac{\\tan(3x)}{x}$.
The form $\\infty/\\infty$ is:
For $f(x,y)=\\dfrac{xy}{x^2+y^2}$, as $(x,y)\\to(0,0)$ along $y=mx$ the limit is:
Because path limits for $xy/(x^2+y^2)$ depend on $m$, the 2D limit:
Evaluate $\\lim_{x\\to\\infty}\\dfrac{5x^3-2}{2x^3+x}$.
Slant asymptote appears for rational $f$ when:
Evaluate $\\lim_{x\\to(\\pi/2)^-}\\tan x$.
If $f$ is continuous on $[0,1]$ with $f(0)=-1$ and $f(1)=2$, then $f(c)=0$ for some $c$ in:
If $a_n=\\dfrac{n}{n+1}$, then $\\lim a_n$ equals:
Geometric $\\sum_{n=0}^{\\infty}(1/3)^n$ sums to:
If $a_n\\not\\to 0$, then $\\sum a_n$:
Harmonic series $\\sum 1/n$:
p-series $\\sum 1/n^p$ converges when:
Telescoping $\\sum_{n=1}^{N}\\left(\\dfrac{1}{n}-\\dfrac{1}{n+1}\\right)$ equals:
A sequence converges to $L$ means eventually terms are:
$\\sum_{n=0}^{\\infty}3\\left(\\dfrac{2}{5}\\right)^n$ equals:
Partial sum $s_N=\\sum_{n=1}^{N}a_n$. Series converges when:
For $|r|\\ge 1$, geometric $\\sum r^n$:
$a_n=(-1)^n$ has limit:
$\\sum 1/n^2$:
If $s_N=2-1/N$, then $\\sum a_n$ converges to:
Geometric first term $a=4$, ratio $r=-1/2$. Sum is:
Bounded monotonic sequences:
Ratio test: if $L=\\lim|a_{n+1}/a_n|<1$, then $\\sum a_n$:
If the ratio limit is $L=1$, the test is:
Alternating series $\\sum(-1)^{n+1}b_n$ with $b_n\\downarrow 0$:
Absolute convergence means:
Alternating harmonic series is:
Root test uses:
For $\\sum\\dfrac{(-1)^n}{n}$, AST error after $N$ terms is at most:
Power series $\\sum c_n(x-a)^n$ has radius $R$ when ratio gives:
Interval of convergence of $\\sum x^n$ is:
Limit comparison with positive $b_n$: if $\\lim a_n/b_n=c\\in(0,\\infty)$, then:
Ratio for $\\sum n!/n^n$: $L$ equals:
Direct comparison: if $0\\le a_n\\le b_n$ and $\\sum b_n$ converges, then:
Radius of $\\sum\\dfrac{(x-2)^n}{n\\,3^n}$ is:
At an endpoint, a power series may:
If $\\sum|a_n|$ converges, then $\\sum a_n$:
Distance between $(1,2)$ and $(4,6)$ is:
Circle $(x-1)^2+(y+2)^2=9$ has radius:
Midpoint of $(0,0)$ and $(4,6)$ is:
Parabola $y^2=8x$ has focus:
Ellipse $x^2/25+y^2/9=1$ has vertices at:
Eccentricity of a circle is:
Complete the square: $x^2+y^2-4x+6y=0$ is circle center:
Directrix of $y^2=4ax$ is:
For ellipse $x^2/a^2+y^2/b^2=1$ ($a>b$), $c=\\sqrt{a^2-b^2}$. Eccentricity is:
Hyperbola $x^2/9-y^2/16=1$ has asymptotes:
Standard circle center $(h,k)$ radius $r$:
Parabola $x=y^2$ opens:
Foci of $x^2/16+y^2/9=1$ are at:
Radius of $x^2+y^2=49$ is:
Definition: parabola is locus of points equidistant from:
Discriminant $B^2-4AC<0$ (nondegenerate) indicates:
$B^2-4AC=0$ indicates:
$B^2-4AC>0$ indicates:
Polar conic $r=\\dfrac{ed}{1-e\\cos\\theta}$ with $e=1$ is a:
If $e>1$ in that polar form, the curve is a:
Rotation of axes is used when the $xy$ term:
Parametric circle $x=3\\cos t$, $y=3\\sin t$ has radius:
Reflective property of a parabola: rays parallel to the axis reflect toward:
Translate $X=x-2$, $Y=y+1$. Point $(x,y)=(2,-1)$ maps to:
For $e=0$ polar conic you get a:
General second-degree $Ax^2+Bxy+Cy^2+Dx+Ey+F=0$ represents a conic when:
Ellipse eccentricity satisfies:
Parametric $x=a\\cosh t$, $y=b\\sinh t$ traces a:
After translating to remove linear terms, an ellipse equation looks like:
Satellite dish design uses which reflective property?
